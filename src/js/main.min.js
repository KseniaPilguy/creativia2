/* burger-menu */

var btn = document.getElementById('navbar-toggler');
var menu = document.querySelector('.navbar-nav');
btn.onclick = function() {
    menu.classList.toggle('show');

};

/* slider - carousel */

var slideIndex = 1;
showSlides(slideIndex);
autoSlider();

function plusSlider() {
    showSlides(slideIndex += 1);
}

function minusSlide() {
    showSlides(slideIndex -= 1);
}

function currentSlide(n) {
    showSlides(slideIndex = n);
}

function showSlides(n) {
    var i;
    var slides = document.getElementsByClassName("item");
    if (n > slides.length) {
        slideIndex = 1;
    }
    if (n < 1) {
        slideIndex = slides.length;
    }
    for (i = 0; i < slides.length; i++) {
        slides[i].style.display = "none";
    }
    slides[slideIndex - 1].style.display = "flex";
}

function autoSlider() {
    var i;
    var slides = document.getElementsByClassName("item");
    for (i = 0; i < slides.length; i++) {
        slides[i].style.display = "none";
    }
    slideIndex++;
    if (slideIndex > slides.length) { slideIndex = 1 }
    slides[slideIndex - 1].style.display = "flex";
    setTimeout(autoSlider, 5000);
}



/* about creativia */


document.querySelector('#accordion').onclick = function(event) {
    let target = event.target;

    if (target.tagName != 'BUTTON') return;
    target.classList.toggle('checked');
    target.parentElement.parentElement.parentElement.childNodes[3].classList.toggle('show');
    target.parentElement.parentElement.parentElement.classList.toggle('open');
}



/* slider - clints */


var slideClintsIndex = 1;
showClintsSlides(slideClintsIndex);
autoClintsSlider();

function currentClintsSlide(n) {
    showClintsSlides(slideClintsIndex = n);
}

function showClintsSlides(n) {
    var i;
    var slidesClints = document.getElementsByClassName("item-clints");
    var dots = document.getElementsByClassName("slider-dots-item");
    if (n > slidesClints.length) {
        slideClintsIndex = 1
    }
    if (n < 1) {
        slideClintsIndex = slidesClints.length
    }
    for (i = 0; i < slidesClints.length; i++) {
        slidesClints[i].style.display = "none";
    }
    for (i = 0; i < dots.length; i++) {
        dots[i].className = dots[i].className.replace(" active", "");
    }
    slidesClints[slideClintsIndex - 1].style.display = "block";
    dots[slideClintsIndex - 1].className += " active";
}

function autoClintsSlider() {
    var j;
    var slidesClints = document.getElementsByClassName("item-clints");
    var dots = document.getElementsByClassName("slider-dots_item");
    for (j = 0; j < slidesClints.length; j++) {
        slidesClints[j].style.display = "none";
    }
    slideClintsIndex++;
    if (slideClintsIndex > slidesClints.length) { slideClintsIndex = 1 }
    for (j = 0; j < dots.length; j++) {
        dots[j].className = dots[j].className.replace(" active", "");
    }
    slidesClints[slideClintsIndex - 1].style.display = "block";
    dots[slideClintsIndex - 1].className += " active";
    setTimeout(autoClintsSlider, 3000);
}